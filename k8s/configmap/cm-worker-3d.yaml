apiVersion: v1
kind: ConfigMap
metadata:
  name: worker-3d-config
  namespace: pfe
data:
  config.yaml: |
    database:
      host: "db.exemple.com"
      port: 5432
      name: "anomaly_detection"
      user: "admin"
      pass: "CHANGE_ME"

    paths:
      images_root: "/home/mario/database-pfe"

    network:
      images_url: "http://images.exemple.com"
