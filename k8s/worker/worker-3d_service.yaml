apiVersion: v1
kind: Service
metadata:
  name: worker-3d-service
  namespace: pfe
  labels:
    app: worker-3d
spec:
  selector:
    app: worker-3d
  ports:
    - port: 8080
      targetPort: 8080
  type: ClusterIP
